import { CircleProgress }:{CircleProgress:any} from "react-gradient-progress";

import React from "react";

function ProgressBar() {
  return <CircleProgress percentage={75} strokeWidth={8} />;
}

export default ProgressBar;
